I’m narrowing this to the highest-value four-day execution path so you can do the minimum set of tasks that actually gets the repo to a working local research agent with real validation.

## The right move for the next four days

Do not try to build the full “research agent platform” from the plan in one shot.

The correct target for this sprint is:

- Use s_agent as the real implementation target
- Keep the system local, artifact-first, and validation-gated
- Treat lg_agent as a future orchestration layer, not the primary runtime this week
- Use qwen-code-3.5-30b only as the planner/code assistant, not as the scientific image model

This is the shortest path to something that actually works.

---

## What matters most

The repo already has the real pipeline foundation in:

- src/data/preprocess.py
- src/data/tile_generator.py
- src/data/dataset_builder.py
- src/data/pipeline.py

Your four-day objective is not “build all advanced research features.” It is:

1. Make the pipeline reproducible
2. Make the agent able to plan and run safe local tasks
3. Validate artifacts before claiming success
4. Demonstrate a complete end-to-end run on a tiny dataset

That is the real win.

---

## Four-day execution plan

### Day 1 — Freeze the architecture and build the agent skeleton
Work in s_agent, not in a broad abstraction.

Do:
- Keep only four core components:
  - planner
  - executor
  - validator
  - memory
- Build explicit JSON task/result schemas
- Make task IDs, run IDs, timestamps, provenance mandatory
- Add a simple approval gate for any command that mutates repo state
- Make the agent able to process a single task end-to-end

Deliverables:
- s_agent/schemas.py with task/result/report models
- s_agent/planner.py with rule-based planner
- s_agent/executor.py with allowlisted local runner
- s_agent/validator.py with artifact checks
- s_agent/memory.py with persistent state log

Acceptance:
- an agent can accept a task, plan it, run it, validate it, and persist a result

---

### Day 2 — Hook the agent to the repo’s real data pipeline
Integrate with the real pipeline, not a fake simulation.

Do:
- Make the planner map tasks to:
  - normalization
  - tile generation
  - dataset build
  - validation
- Wire the executor to approved repo commands
- Add a small “repo context” step so the planner knows the project structure

Minimum real task:
- “Build a tiny dataset from a synthetic or small local FITS-like input”
- Or “Run preprocessing + tile generation on a sample fixture”

Deliverables:
- planner task templates for dataset/preprocess/tile tasks
- repo-aware tool registry
- a working task loop through real repo functions

Do not yet build ML models. This day is about workflow correctness.

---

### Day 3 — Scientific validation + dataset artifact contracts
This is where you convert the agent from a shell wrapper into a research system.

Focus on:
- manifest schema
- tile outputs
- dataset validation
- provenance snapshot

Do:
- Add a manifest artifact that records:
  - dataset name
  - file paths
  - tile count
  - source name
  - config
  - timestamp
- Add validation for:
  - file existence
  - manifest integrity
  - tile count
  - index format
  - normalization sanity
- Add a tiny synthetic dataset test

This is the exact area the repo needs before anything else.

Relevant files:
- src/data/dataset_builder.py
- src/data/pipeline.py
- s_agent/validator.py

Acceptance:
- a built dataset passes a validation report without manual inspection

---

### Day 4 — End-to-end demo + hardening
This is the last day to make it believable.

Do:
- run one full agent loop on a tiny local dataset
- store the run manifest and outputs
- require each run to end in a pass/fail validator report
- write a short runbook describing how to use the agent
- finalize the local runtime and disallow anything outside the allowed tool set

Success condition:
- “I can run the agent, give it a task, and get a validated result in one local workflow”

This is the actual production milestone for the four-day sprint.

---

## What not to do this week

Do not spend time on:
- full multi-agent swarms
- advanced PSF matching
- full MAE training
- large vision foundation models
- full autonomous self-reasoning system
- broad experimentation across many data modalities

Those are all future work, and they do not help the team get a working system in four days.

---

## Model strategy: what to use and what not to use

### Use qwen-code-3.5-30b for:
- planning tasks
- task decomposition
- code suggestions
- repo reasoning
- structured prompt generation

### Do not use it for:
- actual scientific image modeling
- PSF learning
- MAE pretraining
- super-resolution
- image embedding as the primary signal

Use it as the planner/code assistant only.

The actual model stack for the project later should be:
- local planner: qwen-code-3.5-30b
- image backbone: pretrained ViT-Tiny or Swin-Tiny
- optional self-supervised stage: MAE-style masked patch model
- scientific checks: deterministic validators, not ML

For this four-day sprint, the only “model” you need is the qwen planner plus deterministic validation logic.

---

## The practical action to take right now

### Today, do this exact sequence
1. Pick s_agent as the active implementation
2. Finish the schema layer in s_agent/schemas.py
3. Implement rule-based planner and executor in s_agent/planner.py and s_agent/executor.py
4. Implement validation and persistence in s_agent/validator.py and s_agent/memory.py
5. Wire the pipeline to src/data/pipeline.py
6. Run one tiny end-to-end dataset task
7. Stop and freeze once it validates

That is the success path.

---

## Final verdict

If your goal is “have a working local agent that actually runs the pipeline and validates outputs in four days,” the correct strategy is:

- small local agent
- rule-based planner
- safe executor
- deterministic validator
- memory + provenance
- real repo integration
- no broad ML roadmap this week

That is achievable.

If you want, I can turn this into a precise “today-to-tomorrow” checklist with exact files to edit and the exact order of implementation.