I'll search the repository for pipeline, tiling, normalization, and JWST-related keywords to map current status.

Searched for regex `percentile|tile|tiling|256|normalize|normaliz|pipeline|NIRCam|MIRI|flux|MJy/sr|F444W`, 20 results

I'll open the README and key preprocessing/tiling files to map current pipeline state and tests.

Read [](file:///home/nvidia/projects/Astro-Flow-3D/README.md)

Read [](file:///home/nvidia/projects/Astro-Flow-3D/scripts/data/build_dataset.py)

Read [](file:///home/nvidia/projects/Astro-Flow-3D/src/data/dataset_builder.py)

Read [](file:///home/nvidia/projects/Astro-Flow-3D/src/data/pipeline.py)

Read [](file:///home/nvidia/projects/Astro-Flow-3D/src/data/preprocess.py)

Read [](file:///home/nvidia/projects/Astro-Flow-3D/src/data/tile_generator.py)

Read [](file:///home/nvidia/projects/Astro-Flow-3D/test_absolute_paths.py)

Plan: I'll summarize current status (what the repo already implements) and then list the concrete work items the Cl-1.md document recommends.

**Where We’re At**
- **Data-engineering foundation:** The repo implements a reproducible preprocessing pipeline: see README.md.  
- **Percentile normalization implemented:** `normalize()` uses percentile clipping + min-max scaling — see preprocess.py (preprocess.py).  
- **Tiling pipeline in place:** 256×256 tile extraction with configurable stride is implemented in tile_generator.py and orchestrated by pipeline.py and dataset_builder.py (tile_generator.py, pipeline.py, dataset_builder.py).  
- **Dataset build script & config:** build_dataset.py drives dataset creation from preprocessing.yaml (build_dataset.py).  
- **Reconstruction/validation stage exists:** README and pipeline mention reconstruction-based validation (ensures no tiling artifacts) — see README.md and pipeline flow.  
- **Prototype multi-agent notes & research spec:** The design and research nudges are captured in Cl-1.md (Cl-1.md).  
- **Test pointing to recommended normalization:** test_absolute_paths.py encodes a research delta example using asinh stretching as a claimed improvement over percentile clipping (test_absolute_paths.py).

**What Needs To Be Done (per Cl-1.md)**
- **Replace or add flux-preserving normalization:** Implement a flux-preserving normalization (e.g., asinh stretch or other surface-brightness‑aware transform) as an alternative to percentile clipping so inter-band flux ratios are preserved. Add as a configurable option in `normalize()` and config.  
- **PSF-aware multi-band alignment / homogenization:** Add PSF matching or learned PSF‑aware alignment before stacking multi-band images (classical convolution-kernel approach and/or learned cross-resolution modules). This must handle NIRCam/MIRI resolution mismatch.  
- **Multi-channel tensor generation:** Extend dataset building to produce multi-band tensors (co-registered channels + metadata about units/zero-points/PSF). Update `dataset_builder` + index manifest to include band/PSF metadata.  
- **Tile-boundary mitigation:** Switch to overlapping tiles with weighted blending (Gaussian or learned blending) and verify reconstruction preserves subpixel flux ratios. Update tile generation and reconstruction tests.  
- **Self-supervised pretraining strategy:** Add masked-band and masked-patch pretraining (MAE-style) where entire bands can be masked to force learning inter-band relations. Integrate a pretraining experiment recipe in `training/`.  
- **Simulation-based supervision pipeline:** Add support to render simulations (IllustrisTNG/FIRE) through JWST filter+PSF+noise pipeline to create ground-truth labels for physical parameters and train neural SED emulators / SBI models.  
- **Science-aware validation metrics:** Expand validators to check physical invariants (e.g., preservation of band ratios, flux conservation after resampling/deconvolution), not only pixel-wise losses. Implement these checks as part of the pipeline tests.  
- **Agent / orchestration design (tooling & safety):** Implement the research-to-code agent architecture described: planner, executor, validator, memory writer. Use LangGraph (planning), Modal/RunPod (GPU execution), simple JSON/SQLite project memory, and require PRs (no direct pushes to `main`). Start by hardcoding planner + memory writer and manually-run GPU jobs before automating.  
- **Model architecture prototype:** Prototype a multi-band multimodal encoder (ViT or hybrid CNN+ViT) with learned cross-attention for high/low resolution channels; and a PSF‑aware super-resolution branch for MIRI→NIRCam alignment.  
- **Experiment tracking & manifesting:** Add experiment tracking (W&B or MLflow) and extend manifest.json / index.csv with experiment/versioning metadata.  
- **Documentation & tests:** Update configs and docs to document normalization choices, PSF handling strategy, tile stride/overlap defaults, and add unit/integration tests for new normalization and PSF steps.

If you want, I can next:
- (A) Propose concrete code changes for `normalize()` to add an `asinh` option and unit tests, or  
- (B) Sketch the multi-band dataset manifest and the minimal API for PSF metadata in the tile index.

Which would you like me to do first?
